
# Storing the numbers with a function

numList = list()


def storeNumber(num):
    numList.append(num)


# As asked for

for t in range(1,11):
    num = input("Please enter a number: ")
    if num > 0 and num % 2 == 0:
        # print("%d" % num)
        storeNumber(num)

print(numList)
